test-framework think

server provisioning

think about scale to zero
	idea: seperate go program - takes containerID
		pauses
		loads ebpf program - uses cgroup_skb/ingress to detect packet (use cilium/ebpf)
		back to userspace, unpauses container

	should really replay the packets, but for now - screw it
