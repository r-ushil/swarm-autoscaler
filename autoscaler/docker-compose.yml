services:
  webserver:
    image: httpd:latest
    ports:
      - "8080:80"
    labels:
      autoscaler.handlerNode: "lucario"
    deploy:
      placement:
        constraints:
          - node.hostname==lucario
